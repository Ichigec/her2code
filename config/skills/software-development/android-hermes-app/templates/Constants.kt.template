// Template for Constants.kt — the central config file for Hermes Android app.
// KEY PITFALL: DEFAULT_API_URL must match the actual Hermes API endpoint.
// For User: http://<YOUR_LOCAL_IP>:8643 (socat forwarding to Hermes on 8642).

object Constants {
    const val DEFAULT_API_URL = "http://<YOUR_LOCAL_IP>:8643"  // ← CHECK THIS FIRST on connection errors
    const val DEFAULT_MODEL = "hermes-agent"
    const val MAX_MESSAGE_LENGTH = 4000

    // 15 Personas (from ~/.hermes/config.yaml → agent.personalities)
    // These affect STYLE/TONE via system prompt
    val PERSONAS = listOf(
        "default" to "🎭 Standard",
        "technical" to "🔧 Technical",
        "concise" to "💬 Concise",
        "creative" to "🎨 Creative",
        "helpful" to "🤝 Helpful",
        "teacher" to "📚 Teacher",
        "philosopher" to "🏛️ Philosopher",
        "noir" to "🕵️ Noir",
        "shakespeare" to "📜 Shakespeare",
        "pirate" to "🏴‍☠️ Pirate",
        "surfer" to "🏄 Surfer",
        "catgirl" to "🐱 Catgirl",
        "kawaii" to "🌸 Kawaii",
        "uwu" to "🐾 UwU",
        "hype" to "🔥 Hype"
    )

    // 10 Agent Presets (from ~/.hermes/hermes-agent/agent/agents.py)
    // These affect TOOLS/CAPABILITIES via system prompt
    val AGENTS = listOf(
        "general" to "🤖 General",
        "build" to "🔨 Build",
        "plan" to "🧠 Plan",
        "review" to "🔍 Review",
        "safe" to "🛡 Safe",
        "explore" to "🧭 Explore",
        "scout" to "🔭 Scout",
        "deep-explore" to "🔬 Deep Explore",
        "claw" to "🐾 Claw",
        "composter" to "🍂 Composter"
    )

    // System prompts — sent as role:system messages in API requests
    // Order: agent prompt → persona prompt → custom system prompt
    val AGENT_PROMPTS: Map<String, String> = mapOf(...)
    val PERSONA_PROMPTS: Map<String, String> = mapOf(...)

    // Models list
    val MODELS = listOf(
        "hermes-agent" to "Hermes Agent (авто)",
        "deepseek-chat" to "DeepSeek Chat",
        "deepseek-reasoner" to "DeepSeek Reasoner (R1)",
        "gpt-4o" to "GPT-4o",
        ...
    )
}
