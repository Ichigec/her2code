# Cron Job: diffusiongemma-rl-training (FAILED)

**Job ID:** fa51debf78ea
**Run Time:** 2026-07-15 02:33:25
**Schedule:** once in 1m

## Prompt

[IMPORTANT: You are running as a scheduled cron job. DELIVERY: Your final response will be automatically delivered to the user — do NOT use send_message or try to deliver the output yourself. Just produce your report/output as your final response and the system handles the rest. SILENT: If there is genuinely nothing new to report, respond with exactly "[SILENT]" (nothing else) to suppress delivery. Never combine [SILENT] with content — either report your findings normally, or say [SILENT] and nothing more.]

Run the DiffusionGemma RL training pipeline. Do the following:

1. Kill any existing diffusion-server processes and free port 8646.

2. Start the diffusion server:
   ```
   DG_MODEL_PATH="/home/user/.lmstudio/models/yabaimimi/edwixx__diffusiongemma-26B-A4B-it-HERETIC-Uncensored-GGUF/edwixx__diffusiongemma-26B-A4B-it-HERETIC-Uncensored-FP16.gguf" DG_BINARY="/tmp/llama-diffusion-fresh/build/bin/llama-diffusion-cli" DG_NGL=99 DG_CTX_SIZE=65536 DG_PORT=8646 DG_DEFAULT_STEPS=16 /home/user/.hermes/hermes-agent/venv/bin/python3 /home/user/dev/Opencode/diffusion-server.py &
   ```

3. Wait for server health (up to 60s polling http://localhost:8646/health).

4. Run the training: cd /home/user/dev/rldiffusion && /home/user/.hermes/hermes-agent/venv/bin/python3 -u scripts/self_play_loop.py

5. After training exits, kill the server. Report final metrics from the log file at /home/user/dev/rldiffusion/logs/ (most recent training_*.jsonl).

The training takes ~18 min/step × 100 steps ≈ 30 hours. Let it run to completion. Do NOT stop early.

## Error

```
RuntimeError: HTTP 500: litellm.InternalServerError: InternalServerError: OpenAIException - Connection error.. Received Model Group=diffusiongemma-abliterated
Available Model Group Fallbacks=None
```
